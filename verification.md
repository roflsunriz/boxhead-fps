# 検証手順

## 樹木更新の確認結果（2026-09-06）

- 型検査、lint、整形確認、ビルド、依存監査に合格。脆弱性検出なし。
- 最終ビルドのゲームE2Eは92項目合格、失敗0。
- 1920×1080、1366×768、768×1024、390×844で画面のはみ出し・主要UIの重なりなし。
- 広葉樹2個体とヤシを2方向から目視確認。ビルド済みゲームに都市の広葉樹60本・ビーチのヤシ22本が接続されていることと、各ゲームシーンの外観を確認。

## ゲーム全体

`bun run type-check`、`bun run lint`、`bun run format:check`、`bun run audit`、`bun run build` を実行する。次に開発サーバーを停止し、`bun run preview` でビルド成果物を8787番ポートに配信して `bun run test` と `node test/responsive.js` を実行する。失敗時はテスト名と実際の配信元を確認する。詳細は [更新手順](how-to-update.md) を参照。

## 樹木

開発サーバーで `node test/capture-tree-review.js` を実行する。Google Chromeが必要。`art/tree-front-review.png` と `art/tree-side-review.png` を再生成し、次を目視確認する。

ビルド成果物の配信中は `node test/capture-tree-review.js --game` で都市・ビーチの実ゲームシーンを撮影する。`art/tree-city-review.png` と `art/tree-beach-review.png` に保存する。レビュー撮影ではカメラを樹木の前へ移動するため、実際の移動・当たり判定はE2Eでも確認する。

- 都市の広葉樹はケヤキ・クスノキ風の横へ広がる樹冠を持ち、幹から大枝・小枝へ分岐して葉につながる。
- 広葉樹の葉は一枚ずつ幅のある輪郭が見え、葉の間に空間がある。別のシードでも枝の接続が保たれ、枝の方向や葉色に変化がある。
- ヤシの小葉は葉軸の左右へ伸び、先が細く垂れる。裏側からも表示される。
- 根元が地面に接し、葉と枝の影が落ちる。
- 都市とビーチをゲーム内で読み込み、描画エラー、移動、衝突判定の退行がない。

黄金角による配置、階層的な枝分かれ、葉の間隔は自然な見た目のための近似であり、生物学的な完全再現や日射最適化の検証対象ではない。生成形状は静的で、風による揺れは含まない。描画負荷は画面内の木の本数と端末で変わるため、レビュー画面の計測値をゲーム全体のfpsとみなさない。

## 低速なヘッドレス環境での対策

ソフトウェア描画では1フレームが1秒を超えることがある。反撃テストで索敵更新を10秒止めると、発砲前に視認情報の有効期限（0.45秒）が切れる。被弾直後の振り向きと攻撃者の認識を同期的に確認した後は、通常の索敵更新を許可する。反撃の発砲数増加と巡回の3m超の移動は、固定ミリ秒の経過ではなく実際の状態を15秒以内で待つ。期限内に成立しない場合は失敗として扱う。
