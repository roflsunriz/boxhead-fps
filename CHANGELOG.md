# Changelog

すべての変更はこのファイルに記録する。形式は Keep a Changelog に従う。

## [Unreleased]

### Added

- リロード操作の手応えを高めるため、補助手が古い弾倉を引き抜いて画面外へ捨て、新しい弾倉を銃へ差し戻す一人称アニメーションを追加した。
- プレイヤーによる撃破を即座に確認できるように、倒した敵の名前を含むシステムメッセージを画面上部へ追加した。連続撃破は最大3件まで表示し、言語切替時は表示中の通知も再翻訳する。
- ビーチの海が実際に波打って見えるように、3方向のGerstner風波形による水平・垂直頂点変位、浅瀬で高くなる波頭、動的法線、沖から岸へ順番に進む4本の白波帯を追加した。
- 遠距離射撃を避けながら接近できるように、全環境へ高さ2m以上の遮蔽物を追加した。都市にはコンテナ・L字防壁・タンク、ビーチには大型岩・木製防壁・監視小屋、地下には大型箱・防爆壁・タンクを高密度で配置した。
- 死亡時の衝撃と状態を明確に伝えるため、ヘルスがゼロになると前・後・左・右のいずれかへランダムに倒れ、カメラが地面まで落ちながら画面全体が深い赤に染まる演出を追加した。リスポーン時は視点と赤画面を即座に通常状態へ復帰する。
- 戦闘時の手応えと状況把握を改善するため、Web Audio API による射撃音・被弾音・取得音・バリア充填音・爆発音と、攻撃元の方向を示す円周ダメージインジケーターを追加した。
- 戦術的な姿勢変更として、C のしゃがみトグルと X のうつぶせトグルを追加し、姿勢に応じて視点高と移動速度が滑らかに変化するようにした。
- 継戦能力を増やすため、体力とは別のバリア、F でセルを消費する充填アニメーション、バリア・ヘルス・グレネードのランダムマップアイテムを追加した。
- 範囲攻撃手段として、最大 5 個所持できる G の投射グレネード、放物運動・地面反射・時限爆発・距離減衰ダメージを追加した。
- 新しい操作の退行を検出するため、フルオート射撃、姿勢トグル、バリア充填、グレネード所持数、ランダムアイテム生成、方向インジケーターを E2E テスト対象へ追加した。

### Changed

- 遠距離から一方的に狙撃される状況を減らすため、ボットの最大交戦距離を65mから42mへ短縮した。12m以遠では距離に応じて命中率を二次曲線で低下させ、発射間隔を延長し、1発の最大ダメージも抑えた。
- 銃をクリック長押し中に約 9 発/秒で連射するフルオート方式へ変更した。弾切れ時は従来どおり自動リロードへ移行する。

- ウェーブ制のロボット襲撃から、ウェイポイント制のチームデスマッチ(4 vs 3 + プレイヤー)に変更した。
  - ボットはマップ上のウェイポイントグラフ(BFS 経路探索)を移動し、障害物を迂回する。プレイヤーの居場所を常に知っているわけではなく、視界(FOV・射程・障害物による LOS ブロック)に入った敵のみ記憶し、一定時間で忘れる。
  - ボット個別に 体力 100 / 弾数 30 / リロード 2.4 秒 / 視覚 / スプリント / ジャンプ を持ち、弾切れや安全時には自動リロードする。
  - ボットごとにスキルランク(Beginner〜Pro)がランダム割り当てされ、反応速度・命中率・発射間隔・回避行動・記憶時間が異なる。高スキルのボットは被弾時にジグザグ ストレイフやジャンプで幾何学的に弾を避ける。
  - マッチは先に 20 キルしたチームの勝利。プレイヤーは死亡後 3 秒で自陣にリスポーンする。HUD のスコア表示と結果オーバーレイをチームスコア形式に変更した。
- 都市マップの障害物を大幅に追加した(建物 18 棟 + ジャンプ可能な低壁 10 面)。ビーチとアンダーグラウンドにも既存の cover を維持。
- 「Beach Sunset」「Underground」の大気プリセットが、空や霧だけでなくマップ全体の環境を変えるようにした。
  - ビーチ: 砂地テクスチャの地面、波打つ海(頂点アニメーション)、寄せる波の泡、濡れ砂、岩場、曲がったヤシの木(葉・ココナッツ付き)、ヒトデや貝殻、海上に跳ねる魚。深海域への進入は見えない壁で制限される。
  - アンダーグラウンド: コンクリート床・天井・壁に完全囲まれた地下空間。柱のグリッド、天井の配管、吊り下げランプ(7 灯のポイントライト)、木箱の障害物。霧で奥が見えない閉鎖感を演出し、移動範囲も壁で制限される。
- 環境は `src/world.ts` の環境バリアント機構(city / beach / underground)として実装し、プリセット切替時に旧環境の解放と再構築を行う。衝突判定リストも環境ごとに入れ替わる。
- `collide()` を拡張し、大きな障害物ボックスの内部に完全に入り込んだ場合でも最寄り面へ押し出すようにした(従来は押出しベクトルがゼロになり抜け出せなかった)。
- 弾のトレーサー発射起点をカメラから銃身先端のマズル位置へ変更した。マズルは銃モデルの子オブジェクトとして配置し、リコイル演出にも追従する。マズルフラッシュの光源も同位置に移動した。着弾判定は引き続き画面中央の照準レイで行う。

### Added

- 言語切替時に表示中の全 HUD を即時再描画するようにした。i18n モジュールに変更通知フック(`onChange`)を追加し、オーバーレイは表示パラメータ(画面種別・メッセージ生成関数・ボタンラベルキー)を保持して再描画、弾薬カウンタも言語切替の瞬間に再描画する。ゲームオーバー画面を開いたまま JA/EN を切り替えても文言が混在しない。
- UI を日本語 / 英語の 2 言語対応にした。ブラウザの言語設定を自動検出し、画面右上の JA/EN ボタンで切替できる。選択は localStorage に保存する。E2E テストのオーバーレイ判定は翻訳文字列に依存させず、安定した `data-screen` 属性で行うようにした。
- `src/main.ts` を責務ごとに分割した(world / weather / enemies / ui / i18n / textures / types / dom / random)。ファイルサイズ上限と保守性を満たすため。
- `test/responsive.js` を追加した。4 種類のビューポート(1920x1080 / 1366x768 / 768x1024 / 390x844)で横スクロール発生と HUD 要素の視界内収まりを実測検証する。
- README.md を追加した。セットアップ、開発、ビルド、検証手順とモジュール構成を記載した。
- TypeScript を導入し、ゲーム本体を `src/main.ts` へ移行した。`strict` モードで型付けし、`any` は使用せず、天気プリセット・敵・障害物・デバッグ API などを具象インターフェースで型定義した。型の緩さではなく構造で表現するため、素材色のヒットフラッシュ用状態は `HitFlashMaterial` 型へ集約した。
- ESLint(flat config + typescript-eslint)と Prettier を追加した。`@typescript-eslint/no-explicit-any` を error とし、any の混入を機械的に防ぐ。
- Vite 開発サーバ(`bun run dev`)を追加した。HMR 付きの開発モードを提供するため。
- 依存関係の脆弱性スキャン `bun run audit`(bun audit)を追加した。
- Three.js 製のブラウザ FPS ゲーム本体を追加した。WASD 移動、マウス視点(ポインタロック)、射撃、リロード、ウェーブ制の敵スポーン、スコア・体力 HUD を含む。
- 敵・建物・木との衝突判定を追加した。敵が障害物を貫通しないよう、共通の `collide()` 関数をプレイヤーと敵で共用している。
- マップ生成をシード固定 RNG(mulberry32)に変更した。障害物配置を決定論的にし、テストの安定性と再現性を確保するため。
- 大気プリセット機能(7 種: Sunny / Cloudy / Rainy / Thunderstorm / Night / Beach Sunset / Underground)を追加した。読み込みごとにランダム選択され、空・霧・光照・雨・雷フラッシュ・星空・懐中電灯強度が切り替わる。
- カメラ追従の懐中電灯(SpotLight)を追加した。暗い大気プリセットで視界が確保できない問題への対策として、プリセットの暗さに応じて強度を変えている。
- Playwright による E2E テスト群(`test/game.test.js`)を追加した。起動、ポインタロック、移動・衝突・ストレイフ方向、射撃・弾薬・リロード、レイキャスト命中とスコア加算、HUD 同期、ゲームオーバー、フォールバック視点モード、敵の障害物衝突、大気プリセット適用を検証する。

### Fixed

- 環境切替後もボットが旧マップのウェイポイント番号を参照して描画ループを停止させないように、経路番号を検証して無効なら再探索する処理を追加した。
- 検証用の連続射撃撮影スクリプトが lint で停止しないように、未使用式になっていた非同期操作を明示的な待機処理へ修正した。
- ストレイフ(A/D)キーの移動方向が反転していた不具合を修正した。右ベクトルの計算式が逆だったため、`forward × up` に基づく正しい式へ置き換えた。
- ポインタロックの取得に失敗した環境(iframe 内表示、Esc 直後の再ロック拒否など)で視点操作が完全に死ぬ、または画面端で視点回転が止まる不具合を修正した。ロック不可時でも `movementX/Y` によるフォールバック視点操作を有効化し、ロック解除時は PAUSED 画面を出して再開できるようにした。
- `requestPointerLock()` の promise 拒否が未処理例外になる箇所を修正した。
- プレイヤーの初期向きがマップ中央の逆向きだった不具合を修正した。
- 言語切替ボタンがオーバーレイ表示中にクリックできない問題を修正した。`#hud` のスタッキングコンテキスト内では子の z-index がオーバーレイより優先されないため、ボタンを `#hud` 外へ移動し最前面に配置した。
- 影が設定されていたにもかかわらず `shadowMap.enabled` が無効で影が描画されていなかった問題を修正し、ソフトシャドウと ACES トーンマッピングを有効化した。

### Changed

- ゲームモデルをリッチ化した。建物は窓グリッド入りの手続きテクスチャ壁面に、木は幹 + 3 段の葉葉層に、銃は 10 パーツの構成に、敵は手足を振る歩行アニメーションする人型ロボットにそれぞれ置き換えた。
- Three.js の読み込みを CDN(importmap)から npm 依存 + バンドラ解決へ変更した。オフライン・CDN 障害時でも起動するようにするため。
- 本番バンドルを Bun のバンドラ(`bun build ./index.html`)で生成するようにした。Vite は開発サーバ専用とし、rollup / rolldown は使用しない。
- E2E テストの実行対象をソース直配信からビルド済み `dist/` 成果物に変更した。配信される実ファイルを検証するため。
